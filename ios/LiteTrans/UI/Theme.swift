import SwiftUI

/// Accent color comes only from the Asset Catalog (`AccentColor`).
/// Do not hardcode hex literals in Swift UI code; use `Color.accentColor`.
enum Theme {
    static let accent = Color.accentColor
}
